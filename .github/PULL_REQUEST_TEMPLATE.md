## Summary
Describe the change and why it is needed.

## Checklist
- [ ] Tests added or updated (run `pytest` locally)
- [ ] Model changes validated with `scripts/validate_model.py` (if applicable)
- [ ] `requirements.txt` updated if dependencies changed

If this PR updates the model (`*.h5` or `labels.txt`), include the validation output in the description.
